mwptools as of 2016-01-01 also builds and runs on FreeBSD (tested on
11.0 - 14.1).

Just the small matter of installing all the dependencies (the fedora
or ubuntu documents should provide the necesary information).
