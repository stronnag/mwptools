
build:
	cargo build --release

install:
	cargo install --path . --root $$HOME/.local/ --force

debug:
	cargo build

windows:
	cargo build --target x86_64-pc-windows-gnu --release

clean:
	cargo clean
