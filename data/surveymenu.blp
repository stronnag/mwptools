using Gtk 4.0;

menu as_popup {
  section {
    item {
      custom: 'label';
    }
  }
  section {
    item {
      label: 'Insert';
      action: 'survey.asinsert';
    }
    item {
      label: 'Delete';
      action: 'survey.asdelete';
    }
  }
}

menu as_menu {
  section {
    item {
      label: 'Load from text file';
      action: 'asfiles.asload';
    }
    item {
      label: 'Save as text';
      action: 'asfiles.assave';
    }
    item {
      label: 'Save as KML';
      action: 'asfiles.askml';
    }
  }
}
